export const Defaults = {
  BCRYPT_ROUNDS: 12,
  PAGE: 1,
  LIMIT: 20,
  MAX_LIMIT: 100,
  REFRESH_TOKEN_EXPIRY: "7d",
  REFRESH_TOKEN_REMEMBER_ME_EXPIRY: "30d",
  RESET_TOKEN_EXPIRY_MS: 60 * 60 * 1000,
  LOCKOUT_DURATION_MS: 30 * 60 * 1000,
  MAX_LOGIN_ATTEMPTS: 5,
} as const;
